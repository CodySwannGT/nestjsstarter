/**
 * Vitest Setup - Global Test Configuration
 *
 * All test files now use native Vitest APIs (vi.fn(), vi.mock(), vi.spyOn(), etc.).
 * @see https://vitest.dev/config/#setupfiles
 * @module vitest.setup
 */
