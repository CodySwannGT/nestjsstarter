# This file is partly managed by Lisa.
# The keys Lisa owns are listed below; other keys you add are preserved on update.
project_doc_max_bytes = 65536

[features]
codex_hooks = true

# BEGIN lisa-cross-pollinate: mcp:project (source: .mcp.json)
# Regenerated by `lisa cross-pollinate` — edit .mcp.json, not this block.
[mcp_servers.sonarqube]
command = "docker"
args = ["run", "-i", "--rm", "-e", "SONARQUBE_TOKEN", "-e", "SONARQUBE_ORG", "mcp/sonarqube"]

[mcp_servers.sonarqube.env]
SONARQUBE_TOKEN = "${SONAR_TOKEN}"
SONARQUBE_ORG = "${SONAR_ORG}"
# NOTE: the "atlassian" SSE server from .mcp.json is not mirrored here —
# remote/SSE MCP support in Codex is still gated; add it manually if your
# Codex build supports url-based servers.
# END lisa-cross-pollinate: mcp:project
