services:
  valkey:
    image: valkey/valkey:8-alpine
    container_name: thumbwar-valkey
    ports:
      - '6379:6379'
    volumes:
      - valkey_data:/data
    command: valkey-server --appendonly yes
    healthcheck:
      test: ['CMD', 'valkey-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    networks:
      - thumbwar-network
    restart: unless-stopped

  postgres:
    image: postgres:15-alpine
    container_name: thumbwar-postgres
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: thumbwar
      POSTGRES_PASSWORD: thumbwar_local
      POSTGRES_DB: thumbwar
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U thumbwar -d thumbwar']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - thumbwar-network
    restart: unless-stopped

  backend:
    build:
      context: .
      dockerfile: Dockerfile.local
    container_name: thumbwar-backend
    ports:
      - '3000:3000'
    environment:
      IS_OFFLINE: 'true'
      NODE_ENV: development
      DATABASE_HOST: postgres
      DATABASE_PORT: 5432
      DATABASE_NAME: thumbwar
      DATABASE_USER: thumbwar
      DATABASE_PASSWORD: thumbwar_local
      VALKEY_HOST: valkey
      VALKEY_PORT: 6379
    volumes:
      - ./src:/app/src
    depends_on:
      postgres:
        condition: service_healthy
      valkey:
        condition: service_healthy
    command: ['bun', 'run', '--watch', 'src/main-local.ts']
    networks:
      - thumbwar-network
    restart: unless-stopped

networks:
  thumbwar-network:
    driver: bridge

volumes:
  valkey_data:
    driver: local
  postgres_data:
    driver: local
